 
/*集中管控首页*/

 var HomeYNWLYV8Cef;
    if (!HomeYNWLYV8Cef)HomeYNWLYV8Cef = {};
    
    (function() {
        // 打开入场皮带采样机监控界面
      HomeYNWLYV8Cef.OpenTrainBeltSampler = function(paramSampler) {
        native function OpenTrainBeltSampler(paramSampler);
        OpenTrainBeltSampler(paramSampler);
      }; 

        // 打开出场皮带采样机监控界面
      HomeYNWLYV8Cef.OpenOutTrainBeltSampler = function(paramSampler) {
        native function OpenOutTrainBeltSampler(paramSampler);
        OpenOutTrainBeltSampler(paramSampler);
      }; 
        
      // 打开火车机械采样机监控界面
      HomeYNWLYV8Cef.OpenTrainMachinerySampler = function(paramSampler) {
        native function OpenTrainMachinerySampler(paramSampler);
        OpenTrainMachinerySampler(paramSampler);
      };  

        // 打开全自动制样机监控界面
      HomeYNWLYV8Cef.OpenAutoMaker = function(paramSampler) {
        native function OpenAutoMaker(paramSampler);
        OpenAutoMaker(paramSampler);
      };  

        // 打开火车入厂翻车机监控
      HomeYNWLYV8Cef.OpenTrainTipper = function(paramSampler) {
        native function OpenTrainTipper(paramSampler);
        OpenTrainTipper(paramSampler);
      };  

        // 打开火车入厂记录查询
      HomeYNWLYV8Cef.OpenWeightBridgeLoadToday = function(paramSampler) {
        native function OpenWeightBridgeLoadToday(paramSampler);
        OpenWeightBridgeLoadToday(paramSampler);
      };  
      
        // 打开汽车入厂重车衡监控
      HomeYNWLYV8Cef.OpenTruckWeighter = function(paramSampler) {
        native function OpenTruckWeighter(paramSampler);
        OpenTruckWeighter(paramSampler);
      };  

        // 打开汽车入厂重车衡监控
      HomeYNWLYV8Cef.OpenTruckOrder = function(paramSampler) {
        native function OpenTruckOrder(paramSampler);
        OpenTruckOrder(paramSampler);
      };  

        // 打开汽车机械采样机监控
      HomeYNWLYV8Cef.OpenTruckMachinerySampler = function(paramSampler) {
        native function OpenTruckMachinerySampler(paramSampler);
        OpenTruckMachinerySampler(paramSampler);
      };   

        // 打开智能存样柜与气动传输监控
      HomeYNWLYV8Cef.OpenAutoCupboard = function(paramSampler) {
        native function OpenAutoCupboard(paramSampler);
        OpenAutoCupboard(paramSampler);
      };  

        // 打开化验室监控
      HomeYNWLYV8Cef.OpenLaboratory = function(paramSampler) {
        native function OpenLaboratory(paramSampler);
        OpenLaboratory(paramSampler);
      };    

      //查看仓位信息
      HomeYNWLYV8Cef.getStorageInfo=function(paramSampler){
      native function getStorageInfo(paramSampler);
      return getStorageInfo(paramSampler);
      };
      
       //查看故障信息
      HomeYNWLYV8Cef.GetHitchs=function(paramSampler){
      native function GetHitchs(paramSampler);
      return GetHitchs(paramSampler);
      };

       //查看汽车设备监控
      HomeYNWLYV8Cef.OpenCarMonitor=function(paramSampler){
      native function OpenCarMonitor(paramSampler);
      return OpenCarMonitor(paramSampler);
      };
      

       //查看故障信息，跳转
      HomeYNWLYV8Cef.OpenEquInfHitch=function(paramSampler){
      native function OpenEquInfHitch(paramSampler);
            OpenEquInfHitch(paramSampler);
      };

         // 打开火车入厂翻车机监控
      HomeYNWLYV8Cef.OpenTrainTipper = function(paramSampler) {
        native function OpenTrainTipper(paramSampler);
        OpenTrainTipper(paramSampler);
      };  

         // 打开火车入厂翻车机监控
      HomeYNWLYV8Cef.OpenPoundInfo = function(paramSampler) {
        native function OpenPoundInfo(paramSampler);
        OpenPoundInfo(paramSampler);
      };  

       //查看故障信息
      HomeYNWLYV8Cef.GetStorageBox=function(paramSampler){
      native function GetStorageBox(paramSampler);
      return GetStorageBox(paramSampler);
      };

       //查看故障信息
      HomeYNWLYV8Cef.OpenInOutInfo=function(paramSampler){
      native function OpenInOutInfo(paramSampler);
            OpenInOutInfo(paramSampler);
      };


       //查看煤场区域信息
      HomeYNWLYV8Cef.getStorageAreaInfo=function(paramSampler){
      native function getStorageAreaInfo(paramSampler);
      return getStorageAreaInfo(paramSampler);
      };
      
       //查看煤场煤堆信息
      HomeYNWLYV8Cef.getStorageStackInfo=function(storageId,startPoint,EndPoint){
      native function getStorageStackInfo(storageId,startPoint,EndPoint);
      return getStorageStackInfo(storageId,startPoint,EndPoint);
      };
      
    })(); 